# 制造服务化（Servitization）指标构建方法设计（改进版）

> 适用于中文上市公司年报文本，可直接作为论文“方法部分”或项目内技术设计文档使用。

---

## 1. 制造服务化类型与关键词词典

### 1.1 13 类制造服务化类型

沿用现有 13 类服务化分类框架（与英文版本对齐）：

1. maintenance_and_repair（维修与维护服务）  
2. spare_parts_support（备件与零部件供应）  
3. leasing_and_rental（租赁与租用服务）  
4. warranty_and_insurance（质保与保险服务）  
5. installation_and_commissioning（安装与调试服务）  
6. technical_support（技术支持与技术服务）  
7. customization_and_r&d_services（定制化 / 研发服务）  
8. distribution_and_procurement（经销 / 分销 / 采购服务）  
9. training_and_consulting（培训与咨询服务）  
10. solutions_system_integration（解决方案与系统集成）  
11. digital_and_streaming_services（数字化 / 平台 / 订阅服务）  
12. performance_based_contracts（绩效 / 按效付费合约）  
13. recycling_and_process_management（回收 / 再制造 / 流程托管）

### 1.2 中文关键词词典（现有实现）

- 代码位置：`src/servitization_cn/config_keywords_cn.py`  
- 结构：
  - `KEYWORDS_CN`: `Dict[str, List[str]]`，为每一类服务配置若干中文短语；  
  - `CATEGORY_TYPE_CN`: `Dict[str, str]`，标注每一类是 `complementing` 还是 `substituting`。
- 本设计将继续使用该词典：
  - 作为 **baseline 规则系统**；
  - 作为 **自动弱标注（distant supervision）** 的种子。

---

## 2. 文本预处理与句子级候选构建

目标：从年报原始文本出发，构造适合训练 / 推理的 **句子级样本集合**。

### 2.1 文本断句与清洗

- 输入：单家公司某一年的年报全文（或经营情况描述部分）。
- 步骤：
  - 使用中文标点（`。！？；` 等）进行初步断句；
  - 去除多余空白、控制字符，保留必要标点；
  - 可选：对极长句子（> N 字）进行二次切分。
- 输出：该公司-年份下的句子集合 A：
  - 结构示例：
    - `A = { (company, year, sent_id, sentence_text), ... }`

### 2.2 基于词典的句子级候选筛选（B_i）

在句子级别使用现有 `KEYWORDS_CN` 做 **预筛选**，构造 13 个候选集合 B_i：

- 对每一类服务 `i`：
  - 在 A 中筛选“包含任一该类关键词”的句子，形成集合 B_i；
  - 允许同一句子出现在多个 B_i（多标签）。
- B_i 的作用：
  - 作为后续 **人工标注的优先区域**（绝大部分正例集中于此）；
  - 作为 **自动弱标注来源**（在没有人工标签时，先给出粗糙标签）。

### 2.3 句子级样本结构

- 建议统一使用多标签结构：

  - 字段示例：
    - `company, year, sent_id, sentence, label_1, ..., label_13`
  - 其中 `label_i ∈ {0,1}` 表示该句是否属于第 i 类服务。

---

## 3. 标注与弱监督样本构建

目标：在控制人工标注成本的前提下，构造足够用于训练/校正模型的句子级多标签数据集。

### 3.1 自动弱标注（可选但推荐）

利用词典和规则先生成一批“弱标签”（噪声较大但成本极低）：

- 规则 1：若句子出现在某类候选集合 B_i，则将该句的 `label_i = 1`；
- 规则 2：应用简单否定和坏上下文排除（与当前 `detector_cn.py` 的逻辑类似）；
- 结果：得到一个规模较大的弱标注数据集 `D_weak`。

该数据集可用于：

- 预训练 / 预收敛一个多标签分类器；
- 或作为半监督学习、对比学习的辅助数据源。

### 3.2 人工精标样本集

在 `D_weak` 基础上抽样，组织人工标注，获得高质量小规模精标集 `D_gold`：

- 抽样策略：
  - 按类别分层抽样，确保每类都有足够样本；
  - 优先抽取：
    - 规则置信度低的句子（边界样本）；
    - 多标签候选（属于多个 B_i 的句子）；
- 标注形式：
  - 对每句，标定 13 类的 0/1 标签；
  - 支持“无服务类”（全 0）情况。

### 3.3 训练集构建策略

- 方案一（推荐）：`D_gold` 为主，`D_weak` 作为数据增强：
  - 先用 `D_weak` 训练若干 epoch，使模型学到基本关联；
  - 再用 `D_gold` 微调，降低弱监督噪声影响。
- 方案二（标注足够时）：仅用 `D_gold` 训练，`D_weak` 作为对比实验或辅助。

---

## 4. 句子级多标签分类模型（改进版）

目标：用 **一个模型** 同时预测 13 类服务标签，而不是 13 个独立模型。

### 4.1 模型结构

- 基础编码器：
  - 选用适合中文的预训练语言模型，如：
    - RoBERTa-wwm-ext, MacBERT, 中文金融 BERT, Erlangshen 系列等。
- 输出层：多标签分类头：
  - 将 [CLS] 向量或句子池化向量输入到一层全连接：
  - 输出向量维度 = 13；
  - 每维通过 sigmoid 得到属于该类的概率 `p_i ∈ [0,1]`。

### 4.2 训练目标

- 损失函数：多标签二元交叉熵（Binary Cross Entropy, BCE），对 13 维分别计算再求和/平均；
- 若结合弱标签：
  - 可对 `D_weak` 使用较低权重（或噪声感知损失）；
  - 对 `D_gold` 使用正常权重作为主监督信号。

### 4.3 预测与决策规则

- 对句子 s，模型输出 13 维 `p_i(s)`；
- 对每类 i，设置阈值 `τ_i`（可统一或按类不同）：
  - 若 `p_i(s) ≥ τ_i`，则认为句子属于第 i 类（`label_i = 1`），否则为 0；
- 阈值可通过验证集上优化 F1 / recall / precision 进行确定。

### 4.4 与规则的融合

为提升稳健性和可解释性，本设计保留现有规则系统，与模型融合：

- **规则优先 / 模型修正**（示例策略）：
  - 若句子命中高置信度关键词（如行业特定术语），则直接将该类置为 1；
  - 模型用于识别：
    - 无明显关键词但语义明显属于某类的句子；
    - 倾向性较强但规则不覆盖的表达。

- **模型优先 / 规则补 recall**（另一种策略）：
  - 若模型给出的某类概率始终偏低或数据稀少，则允许：
    - 在公司-年份聚合时，把该类的高频关键短语出现视作补充证据。

具体融合方式可以在项目实现阶段根据验证结果微调，此处以“规则 + 模型混合系统”为总体方向。

---

## 5. 企业年度服务化指标（SerManu）构建

目标：基于句子级预测结果与规则融合结果，在 **公司 × 年度** 层面构建最终的 SerManu 指标。

### 5.1 公司-年度聚合

对每家企业、每一年：

1. 获取该年报文本的全部句子集合 A；
2. 对每句 s：
   - 运行多标签模型，得到 `p_i(s)`；
   - 应用阈值和/或规则融合，得到句子级标签 `label_i(s) ∈ {0,1}`；
3. 对 13 类服务逐类聚合：
   - 若存在任一 s 使得 `label_i(s) = 1`，则认为该企业该年涉及第 i 类服务：
     - `I_i(company, year) = 1`；
   - 否则：`I_i(company, year) = 0`。

### 5.2 SerManu 指标公式

在公司-年度层面，定义服务化程度指标：

- **SerManu(company, year) = ∑_{i=1}^{13} I_i(company, year)**  
- 即：
  - `I_i` 为第 i 类服务的指示变量：
    - `I_i = 1`：企业在该年度的年报中有证据表明存在第 i 类服务；
    - `I_i = 0`：未观察到该类服务的明确信息。

除 SerManu 之外，可保留/扩展以下辅助指标：

- `service_num`：13 类中被激活的类别数量（与 SerManu 相同）；
- `comp_count`：类别类型为 `complementing` 且激活的数量；
- `sub_count`：类别类型为 `substituting` 且激活的数量；
- `risk_score`：基于 `comp_count` 和 `sub_count` 的简单风险评分，例如：
  - `risk_score = 2.0 * sub_count + 0.5 * comp_count`  （可与现有英文版保持一致）。

---

## 6. 与现有代码的对应关系与改造建议（概要）

### 6.1 直接沿用的部分

- 13 类服务定义与中英文对照：
  - `src/servitization/config_keywords.py`（英文）
  - `src/servitization_cn/config_keywords_cn.py`（中文）
- 公司-年份文本聚合与结果输出：
  - `src/servitization/pipeline.py`（英文）
  - `src/servitization_cn/pipeline_cn.py`（中文）

### 6.2 需要新增或改造的模块（中文为主）

- **句子级处理与导出**：
  - 新增：中文断句函数与句子集合 A 构建模块；
  - 新增：基于 `KEYWORDS_CN` 的句子级筛选与 B_i 构建、抽样导出脚本。

- **多标签模型训练与推理**：
  - 新增：基于预训练中文模型的多标签训练脚本（使用 `D_weak` + `D_gold`）；
  - 新增：模型推理接口，输入句子列表，输出 13 维概率与标签。

- **公司-年度聚合与 SerManu 计算（模型版）**：
  - 在现有 `process_company_item1s_cn` 逻辑基础上，增加一个“BERT 版 / 模型版” pipeline：
    - 先断句、预测句子级标签；
    - 再聚合到公司-年度，生成 `I_i` 和 SerManu。

- **规则与模型融合**：
  - 在 `detector_cn.py` 或相邻模块中，引入简单融合策略；
  - 保留纯规则版结果（便于对比和鲁棒性检查）。

---

## 7. 实施顺序建议（概要）

1. **阶段 1：句子级数据与标注工具**  
   - 实现中文断句 + A / B_i 构建 + 标注 CSV 导出；
   - 在少量公司上跑通、组织人工标注，得到 `D_gold`。

2. **阶段 2：多标签模型训练**  
   - 基于预训练中文模型 + `D_weak` + `D_gold` 训练句子级多标签分类器；
   - 在验证集上调节阈值 `τ_i` 并评估指标（F1、召回等）。

3. **阶段 3：模型集成与指标计算**  
   - 在 pipeline 中集成句子级模型，替代/补充纯词典规则；
   - 在公司-年度层面生成模型版 SerManu，并与规则版对比。

4. **阶段 4：迭代优化**  
   - 根据错误分析，优化：
     - 关键词词典（`KEYWORDS_CN` 更新）；
     - 规则（否定 / 坏上下文）；
     - 模型架构与阈值设置。

---

本设计文档主要定义了从“原始年报文本”到“公司-年度 SerManu 指标”的方法路径，并在保留现有 13 类与规则系统的前提下，引入了 **多标签预训练模型 + 规则融合** 的改进方案。后续代码改造和实验应以本设计为整体框架进行实现与迭代。
